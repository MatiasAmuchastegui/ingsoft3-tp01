# Copiar a .env y ajustar si hace falta.
# Sólo las variables con prefijo VITE_ llegan al código del navegador.
# OJO: todo lo que empiece con VITE_ queda embebido en el bundle público.
# Nunca poner secretos acá.

VITE_API_URL=http://localhost:5080
